# Machine Learning Module (proto)

## Architecture

## Local setup

1. `uv` package manager.
2. `uv add -r requirements.txt`
3. OpenMP must be installed, run `brew install libomp` on MacOS

## Fair Use Policy (NHANES)
